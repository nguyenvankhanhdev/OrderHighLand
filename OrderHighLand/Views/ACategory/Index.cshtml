@model  IEnumerable<OrderHighLand.Models.Category>
@{
	Layout = "_AdminLayout";
}
<section class="section">
	<div class="section-header">
		<h1>All Category</h1>
	</div>
	<div class="section-body">
		<div class="row">
			<div class="col-12">
				<div class="card">
					<div class="card-header">
						
						<div class="card-header-action">
							<a asp-controller="ACategory" asp-action="Create" class="btn btn-primary">
								<i class="fas fa-plus"></i> Create New
							</a>
						</div>
					</div>
					<div class="card-body">
						<table class="table table-bordered table-hover">
							<thead>
								<tr>
									<th>ID</th>
									<th>Name</th>
									<th>Action</th>
								</tr>
							</thead>
							<tbody>
								@foreach (var item in Model)
								{
									<tr>
										<td>@item.Id</td>
										<td>@item.Name</td>
										<td>
											<a href="@Url.Action("Edit", "ACategory", new { id = item.Id })" class="btn btn-primary"> <i class="far fa-edit"></i></a>
											<a href="@Url.Action("Delete", "ACategory", new { id = item.Id })" class="btn btn-danger delete-item"> <i class="far fa-trash-alt"></i></a>
										</td>
									</tr>
								}
							</tbody>
						</table>
					</div>

				</div>
			</div>
		</div>
	</div>
</section>










